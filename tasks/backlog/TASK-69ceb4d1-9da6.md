---
id: TASK-69ceb4d1-9da6
title: Migrate package manager from Yarn 4 to PNPM
status: backlog
priority: high
type: chore
created: 2026-04-02
updated: 2026-04-02
product: harness
depends_on: []
blocks: []
tags: [tooling, dx]
session_estimate: 1
---

# Migrate package manager from Yarn 4 to PNPM

## Objective

Replace Yarn 4 (Berry) with PNPM as the monorepo package manager. All workspace orchestration, dependency resolution, and developer-facing commands must work identically after migration.

## Scope

### Owns
- `package.json` (root) — update `packageManager`, `scripts`, remove `@yarnpkg/types` devDep
- `yarn.config.cjs` — DELETE (Yarn constraints file)
- `yarn.lock` — DELETE
- `.yarnrc.yml` — DELETE
- `.yarn/` — DELETE directory
- `.gitignore` — update Yarn Berry section to PNPM equivalent
- `.gitattributes` — remove Yarn-specific entries
- `pnpm-workspace.yaml` — CREATE (workspace config)
- `pnpm-lock.yaml` — CREATE (generated by `pnpm install`)
- `.npmrc` — CREATE if needed (equivalent of .yarnrc.yml settings)
- `README.md` — update prerequisites, install command, and directory tree references
- `tools/code-memory/README.md` — update `yarn workspace` command references
- `tools/code-memory/src/config.mjs` — update `.yarn` ignore pattern to PNPM equivalent
- `.claude/settings.json` — update `Bash(yarn:*)` permission to `Bash(pnpm:*)`
- `.claude/settings.local.json` — update all `Bash(yarn ...)` permissions to `Bash(pnpm ...)`

### Reads
- `tools/code-memory/package.json` — verify workspace name for pnpm commands
- All `products/*/apps/*/package.json` — verify workspace glob compatibility
- `context/decisions.md` — for recording the migration decision

## Contracts

### Consumes
None

### Publishes
None (internal tooling change, no API contract changes)

## Acceptance Criteria

### Automated
- [ ] `pnpm install` succeeds from a clean state (no `node_modules/`)
- [ ] `pnpm run build` executes workspace builds (same as `yarn workspaces foreach -Apt run build`)
- [ ] `pnpm run test` executes workspace tests
- [ ] `pnpm run check` executes workspace checks
- [ ] No files named `yarn.lock`, `.yarnrc.yml`, or `yarn.config.cjs` exist in the repo
- [ ] No `.yarn/` directory exists (except possibly `.yarn/releases` can be removed)
- [ ] `pnpm-lock.yaml` is committed
- [ ] `pnpm-workspace.yaml` defines the same workspace globs as the old `package.json.workspaces`
- [ ] `package.json` has `packageManager` set to a PNPM version
- [ ] `grep -ri yarn README.md` returns no results (all references updated)
- [ ] `grep -ri yarn tools/code-memory/README.md` returns no results

### Judgment
- [ ] Migration decision recorded in `context/decisions.md`
- [ ] `.gitignore` entries are appropriate for PNPM (no leftover Yarn patterns)
- [ ] `.claude/settings.json` and `.claude/settings.local.json` permissions reference `pnpm` not `yarn`

## Constraints

- Must preserve the `node-modules` linker strategy (no PnP or symlink hoisting that would break native addons like `better-sqlite3` and `tree-sitter`)
- Must preserve workspace topology: `tools/*`, `common/*`, `products/*/apps/*`
- Do NOT change any workspace-level `package.json` files (only root)
- PNPM version should be latest stable (use `corepack enable` + `corepack prepare`)

## Context

### Files to modify (complete inventory)

| File | Action | Details |
|------|--------|---------|
| `package.json` | Modify | `packageManager` field, `scripts` (replace `yarn workspaces foreach -Apt run` with `pnpm -r run`), remove `@yarnpkg/types` and `constraints`/`constraints:fix` scripts |
| `yarn.config.cjs` | Delete | Yarn constraints — no PNPM equivalent needed for now (can add `syncpack` later) |
| `yarn.lock` | Delete | Replaced by `pnpm-lock.yaml` |
| `.yarnrc.yml` | Delete | Settings move to `.npmrc` or `pnpm-workspace.yaml` |
| `.yarn/` | Delete | Yarn Berry cache/state directory |
| `.gitignore` | Modify | Remove `# Yarn Berry` section, no PNPM-specific ignores needed (pnpm store is global) |
| `.gitattributes` | Modify | Remove `/.yarn/**`, `/.pnp.*` entries |
| `pnpm-workspace.yaml` | Create | `packages: [tools/*, common/*, products/*/apps/*]` |
| `.npmrc` | Create | `node-linker=hoisted` (equivalent to `nodeLinker: node-modules`) |
| `README.md` | Modify | Prerequisites, install command, directory tree comment |
| `tools/code-memory/README.md` | Modify | `yarn workspace code-memory test` -> `pnpm --filter code-memory test` |
| `tools/code-memory/src/config.mjs` | Modify | `.yarn` in ignore list -> no change needed (already ignored, but verify) |
| `.claude/settings.json` | Modify | `Bash(yarn:*)` -> `Bash(pnpm:*)` |
| `.claude/settings.local.json` | Modify | All yarn permission patterns -> pnpm equivalents |

### Migration steps (recommended order)
1. Delete Yarn artifacts: `.yarnrc.yml`, `yarn.config.cjs`, `yarn.lock`, `.yarn/`
2. Create `pnpm-workspace.yaml` and `.npmrc`
3. Update root `package.json` (`packageManager`, scripts, devDeps)
4. Run `pnpm install` to generate lockfile
5. Update documentation (README.md, tools/code-memory/README.md)
6. Update `.gitignore` and `.gitattributes`
7. Update `.claude/settings.json` and `.claude/settings.local.json`
8. Update `tools/code-memory/src/config.mjs` if needed
9. Verify: clean install, build, test
10. Record decision in `context/decisions.md`

## Work Log

*(append entries as work progresses)*
